{
  "name": "task-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.11.0",
    "react-beautiful-dnd": "^12.0.0",
    "react-dom": "^16.11.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "concurrently \"react-scripts start\" \"json-server --watch tasks.json --port 3001\"",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "concurrently": "^5.0.0"
  }
}
